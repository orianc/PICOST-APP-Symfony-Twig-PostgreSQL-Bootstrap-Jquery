<nav class="navbar navbar-expand-md navbar-light">


	<button class="navbar-toggler text-dark border-0" type="button" data-toggle="collapse" data-target="#navbarAlbum" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
		<i style="width: 20px;" class="fas fa-ellipsis-h "></i>
	</button>

	<div id="navbarAlbum" class="collapse navbar-collapse col-3 nav nav-albums flex-md-column">

		<div class="bg-light">
			{% for album in albums %}
				<div class="nav-item-link d-flex p-1">
					{% if is_granted('ROLE_ADMIN') %}
						<a class="fs-1" href="{{ path('album_edit', {'id': album.id}) }}">
							<i class="btn-warning rounded p-1 mx-2 fas fa-edit fa-1x"></i>
						</a>
					{% endif %}

					<a class="" href="{{ path('album_show', {'id': album.id}) }}">{{ album.name }}</a>
				</div>
			{% endfor %}
		</div>
	</div>
</nav>
